package com.defang.launcher.ui.theme

import androidx.compose.ui.graphics.Color

// Defang palette — intentionally desaturated and low-contrast.
// The phone should feel less rewarding to use, not more.

val BackgroundDark = Color(0xFF0A0A0A)
val BackgroundLight = Color(0xFFF5F5F5)

val SurfaceDark = Color(0xFF141414)
val SurfaceLight = Color(0xFFFFFFFF)

val OnBackgroundDark = Color(0xFFE0E0E0)
val OnBackgroundLight = Color(0xFF1A1A1A)

val SubtleTextDark = Color(0xFF808080)
val SubtleTextLight = Color(0xFF606060)

// Accent — used sparingly for countdowns and timers only.
// Warm but not red (red = cue; we don't want to be a cue).
val Accent = Color(0xFFB07D4A)
val AccentDisabled = Color(0xFF4A3820)

val DestructiveRed = Color(0xFFB04040)
val SuccessGreen = Color(0xFF4A8C5A)
