The app drawer now keeps up with what is installed: newly installed apps appear and uninstalled ones disappear without a restart — refreshed when you open the drawer, and the moment an app is added or removed. Long-press an app for App info or to uninstall it. Leaving the gate with "Go back" now closes the watched app instead of leaving it running in the background. The F-Droid listing also gains an app icon.
