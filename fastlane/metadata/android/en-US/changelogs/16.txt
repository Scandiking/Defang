Fixes Snapchat opening with no intent gate again. Snapchat had quietly stopped appearing in the standard Android API Defang used to detect it in the background — the second detection method it's evaded, after doing the same to accessibility events a few weeks ago. It now also reads the phone's on-screen window list directly, a channel Snapchat can't opt out of without breaking its own display.
